import { actions as promotionActions, types as promotionTypes } from './action';
import promotionEpic from './epic';
import promotionReducer from './reducer';

export { promotionActions, promotionTypes, promotionEpic, promotionReducer };
