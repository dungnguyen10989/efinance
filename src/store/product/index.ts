import { actions as productActions, types as productTypes } from './action';
import productEpic from './epic';
import productReducer from './reducer';

export { productActions, productTypes, productEpic, productReducer };
